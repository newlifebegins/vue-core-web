module.exports = {
    plugins: {
        autoprefixer: {
            browsers: ['Chrome > 31', 'ff > 31', 'ie >= 8', 'last 10 versions', 'Android >= 4.0', 'iOS >= 7.1'],
            grid: true,
        },
    },
};
